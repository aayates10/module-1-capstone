package com.techelevator;

public class VendingMachine {

    // attributes
    private boolean hasSufficientFunds;
    private boolean isInStock;

    /*
    methods
    checkInventory()
    // reads file, restocks
    TODO Aaron, try something like this in Inventory!
    String[] lineArray = line.split("\\|");
    String setSlotID = lineArray[0];
    String setName = lineArray[1];
    double setPrice = Double.parseDouble(lineArray[2]);
    String setSnackType = lineArray[3];
    Snack snack = new Snack(slotID, name, price, snackType);
    snack.add(snack);

    displayItems()
    // list item names and quantity
    // check if isInStock - if false, print "SOLD OUT"

    // user selects (2) Purchase
    feedMoney()
    print "Insert whole dollar amounts"
    balance += inserted money
    printf("Current Money Provided: $ %.2f" + balance)

    selectProduct()
    if !(getPrice() <= balance) {
        sout("Insufficient funds");

    dispense()

    // print message
    String message = "";
    if (getSnackType.equalsIgnoreCase("chips") {
        message = "Crunch Crunch, Yum!";
    } else if (getSnackType.equalsIgnoreCase("candy") {
        message = "Munch Munch, Yum!";
    } else if (getSnackType.equalsIgnoreCase("drink") {
        message = "Glug Glug, Yum!";
    } else if (getSnackType.equalsIgnoreCase("gum") {
        message = "Chew Chew, Yum!";
    } return message;

    giveChange()

    log()
     */

}
